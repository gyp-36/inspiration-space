<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.is.inspirationspaceclient.work.mapper.WorkStatsMapper">

    <select id="selectWorkSalesRank" resultType="com.is.inspirationspaceclient.work.model.vo.WorkStateVo">
        SELECT 
            w.work_id as workId,
            w.title as title,
            w.total_sales as totalSales
        FROM work_info w
        WHERE w.total_sales > 0
        ORDER BY w.total_sales DESC, w.work_id ASC
        LIMIT #{topN}
    </select>
    
</mapper>